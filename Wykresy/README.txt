1. kNN
	1.1 weights == "uniform"
2. Random Forest
	2.1 max_depth == 15, min_samples_split == 4
	2.2 max_depth == 15, n_estimators == 200
3. SVM RBF
	3.1 gamma == 0.5, max_iter == 300
	3.2 C == 100, max_iter == 300
4. Bayes
5. Gaussian Process
6. Neural Net
	6.1 hidden_layer_sizes == (150,), activation == tanh,
		max_iter == 300
	6.2 alpha == 0.15, activation == tanh, max_iter == 300